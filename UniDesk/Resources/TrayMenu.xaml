<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <SolidColorBrush x:Key="TrayMenuBackground" Color="#F2FFFFFF"/>
    <SolidColorBrush x:Key="TrayMenuBorder" Color="#E2E8F0"/>
    <SolidColorBrush x:Key="TrayMenuText" Color="#334155"/>
    <SolidColorBrush x:Key="TrayMenuTextMuted" Color="#94A3B8"/>
    <SolidColorBrush x:Key="TrayMenuHover" Color="#F1F5F9"/>
    <SolidColorBrush x:Key="TrayMenuDangerHover" Color="#FEF2F2"/>
    <SolidColorBrush x:Key="TrayMenuDangerText" Color="#DC2626"/>

    <Style x:Key="TrayMenuItemStyle" TargetType="MenuItem">
        <Setter Property="Foreground" Value="{StaticResource TrayMenuText}"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="MenuItem">
                    <Border x:Name="ItemBorder"
                            Margin="4,1"
                            Padding="12,8"
                            CornerRadius="8"
                            Background="Transparent">
                        <ContentPresenter ContentSource="Header"
                                          VerticalAlignment="Center"
                                          RecognizesAccessKey="True"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="ItemBorder" Property="Background" Value="{StaticResource TrayMenuHover}"/>
                        </Trigger>
                        <DataTrigger Binding="{Binding Tag, RelativeSource={RelativeSource Self}}" Value="Danger">
                            <Setter Property="Foreground" Value="{StaticResource TrayMenuDangerText}"/>
                            <Setter TargetName="ItemBorder" Property="Background" Value="Transparent"/>
                        </DataTrigger>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding Tag, RelativeSource={RelativeSource Self}}" Value="Danger"/>
                                <Condition Binding="{Binding IsHighlighted, RelativeSource={RelativeSource Self}}" Value="True"/>
                            </MultiDataTrigger.Conditions>
                            <Setter TargetName="ItemBorder" Property="Background" Value="{StaticResource TrayMenuDangerHover}"/>
                        </MultiDataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="TrayContextMenuStyle" TargetType="ContextMenu">
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContextMenu">
                    <Border Background="{StaticResource TrayMenuBackground}"
                            BorderBrush="{StaticResource TrayMenuBorder}"
                            BorderThickness="1"
                            CornerRadius="12"
                            Padding="4"
                            MinWidth="148">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="16"
                                              ShadowDepth="2"
                                              Opacity="0.18"
                                              Color="#000000"/>
                        </Border.Effect>
                        <StackPanel IsItemsHost="True"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="TraySeparatorStyle" TargetType="Separator">
        <Setter Property="Margin" Value="10,4"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Separator">
                    <Border Height="1" Background="{StaticResource TrayMenuBorder}"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
